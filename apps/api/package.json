{
  "name": "@platform/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:deps": "npm run build --workspace @platform/shared",
    "dev": "npm run build:deps && tsx watch src/index.ts",
    "build": "npm run build:deps && tsc -p tsconfig.json",
    "typecheck": "npm run build:deps && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.2",
    "@fastify/multipart": "^9.0.3",
    "@platform/shared": "0.1.0",
    "@supabase/supabase-js": "^2.49.8",
    "@vectorize-io/hindsight-client": "^0.5.1",
    "adm-zip": "^0.5.16",
    "fastify": "^5.3.3",
    "groq-sdk": "^0.26.0",
    "mammoth": "^1.9.0",
    "pdf-parse": "^1.1.1",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.7",
    "@types/node": "^22.15.17",
    "tsx": "^4.19.4"
  }
}
